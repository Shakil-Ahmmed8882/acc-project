import classNames from "classnames";

const HorizontalLine= ({className}) => {
  return (
    <span className={`${classNames} border-t border-[#f2f2f27c] block w-full pt-3 `}></span>
  );
};

export default HorizontalLine; 