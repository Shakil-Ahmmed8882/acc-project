

const HorizontalLine = ({classNames}) => {
  return (
    <span
      className={`${classNames} border-t border-[#f2f2f27c] block w-full  z-[1000]`}
    ></span>
  );
};

export default HorizontalLine;
