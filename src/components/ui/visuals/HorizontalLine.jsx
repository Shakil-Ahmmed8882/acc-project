

const HorizontalLine = ({classNames}) => {
  return (
    <span
      className={`${classNames} border-t border-[#f2f2f27c] block w-full pt-2 z-[1000]`}
    ></span>
  );
};

export default HorizontalLine;
