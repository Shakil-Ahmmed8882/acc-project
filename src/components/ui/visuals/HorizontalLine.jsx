import classNames from "classnames";

const HorizontalLine = () => {
  return (
    <span
      className={`${classNames} border-t border-[#f2f2f27c] block w-full pt-3 z-[1000]`}
    ></span>
  );
};

export default HorizontalLine;
